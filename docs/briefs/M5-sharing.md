# Brief M5 — Sharing & polish

## In scope
- Plugin-bundle export target (plugin.json + hooks/hooks.json + skills/agents) + fixture.
- Import from a plugin directory.
- Effort×model advisor content pass (docs links per warning).
- Marketplace assets: icon, README with GIFs, categories/keywords; publisher setup doc.

## Acceptance
- A gallery template exported as a plugin installs via /plugin and functions.
- vsce publish dry-run clean.
