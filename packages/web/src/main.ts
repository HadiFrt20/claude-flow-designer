// Placeholder web entry — the standalone Vite app lands in M3 (see docs/briefs/M3-web-host.md).
// M0 ships headless-only (core schema + validation). This keeps `npm run build` green.
const root = document.getElementById('root');
if (root) {
  root.textContent = 'Claude Flow Designer — web host arrives in M3.';
}
