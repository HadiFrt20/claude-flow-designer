# Ship only the three built bundles + manifest + README. Exclude everything else
# under dist (tsc typecheck output, sourcemaps, .d.ts, stray compiled modules).
dist/**
!dist/extension.js
!dist/webview.js
!dist/webview.css
src/**
webview/**
test/**
esbuild.mjs
tsconfig.json
tsconfig.tsbuildinfo
**/*.ts
.vscode/**
